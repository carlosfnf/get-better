<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
    <foreignObject width="100%" height="100%">
        <html xmlns="http://www.w3.org/1999/xhtml">
            <head>
                <meta charset="UTF-8" />
                <meta name="viewport" content="width=device-width, initial-scale=1.0" />
                <title>Welcome</title>
                <style>
                    /*<![CDATA[*/

                    /* Registered custom property so the conic border can spin */
                    @property --ang { syntax: '<angle>'; initial-value: 0deg; inherits: false; }

                    body, html {
                        margin: 0; padding: 0; width: 100%; height: 100%;
                        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                        background-color: #050505; color: white;
                        overflow: hidden; transition: background 0.5s ease;
                    }

                    /* ============ VIDEO: slow Ken Burns zoom + color breathing ============ */
                    #video-background {
                        position: fixed; top: 50%; left: 50%;
                        min-width: 100%; min-height: 100%; width: auto; height: auto;
                        z-index: 0; transform: translate(-50%, -50%);
                        object-fit: cover; transition: opacity 0.8s ease;
                        animation: ken-burns 36s ease-in-out infinite alternate;
                    }
                    @keyframes ken-burns {
                        0%   { transform: translate(-50%, -50%) scale(1); filter: saturate(1.05) brightness(0.85); }
                        100% { transform: translate(-50%, -50%) scale(1.14); filter: saturate(1.3) brightness(1); }
                    }

                    /* ============ AMBIENT FX LAYERS ============ */
                    #orb-layer { position: fixed; inset: 0; z-index: 1; overflow: hidden; pointer-events: none; }
                    .orb { position: absolute; border-radius: 50%; will-change: transform; }
                    .orb.o1 { width: 46vw; height: 46vw; min-width: 320px; min-height: 320px; background: radial-gradient(circle, rgba(255,71,87,.20), transparent 70%); top: -12%; left: -8%; animation: orb-a 26s ease-in-out infinite alternate; }
                    .orb.o2 { width: 52vw; height: 52vw; min-width: 340px; min-height: 340px; background: radial-gradient(circle, rgba(110,142,251,.22), transparent 70%); bottom: -18%; right: -10%; animation: orb-b 32s ease-in-out infinite alternate; }
                    .orb.o3 { width: 34vw; height: 34vw; min-width: 260px; min-height: 260px; background: radial-gradient(circle, rgba(167,119,227,.18), transparent 70%); top: 38%; left: 55%; animation: orb-c 21s ease-in-out infinite alternate; }
                    @keyframes orb-a { to { transform: translate(14vw, 20vh) scale(1.3); } }
                    @keyframes orb-b { to { transform: translate(-16vw, -18vh) scale(1.18); } }
                    @keyframes orb-c { to { transform: translate(-22vw, 12vh) scale(1.35); } }

                    #fx-grid {
                        position: fixed; inset: 0; z-index: 21; pointer-events: none;
                        background-image:
                            linear-gradient(rgba(255,255,255,.045) 1px, transparent 1px),
                            linear-gradient(90deg, rgba(255,255,255,.045) 1px, transparent 1px);
                        background-size: 55px 55px;
                        animation: grid-drift 14s linear infinite;
                        -webkit-mask-image: radial-gradient(ellipse at center, black 25%, transparent 72%);
                        mask-image: radial-gradient(ellipse at center, black 25%, transparent 72%);
                    }
                    @keyframes grid-drift { from { background-position: 0 0; } to { background-position: 55px 55px; } }

                    #vignette { position: fixed; inset: 0; z-index: 22; pointer-events: none; background: radial-gradient(ellipse at center, transparent 52%, rgba(0,0,0,.55) 100%); }

                    #mouse-glow {
                        position: fixed; top: 0; left: 0; width: 400px; height: 400px; border-radius: 50%;
                        z-index: 23; pointer-events: none; will-change: transform;
                        background: radial-gradient(circle, rgba(140,150,255,.14), rgba(167,119,227,.08) 40%, transparent 70%);
                    }

                    /* ============ SNOW: depth layers + organic sway + twinkle ============ */
                    #snow-container { position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 20; }
                    .snowflake {
                        position: absolute; top: -70px; opacity: var(--o, .8);
                        filter: drop-shadow(0 0 5px rgba(255,255,255,.8));
                        will-change: transform;
                        animation: fall var(--fdur, 9s) linear forwards;
                    }
                    .snowflake.twinkle { animation: fall var(--fdur, 9s) linear forwards, flake-twinkle var(--tdur, 1.5s) ease-in-out infinite alternate; }
                    @keyframes fall {
                        0%   { transform: translate(0, -12vh) rotate(0deg); }
                        25%  { transform: translate(var(--sway, 20px), 25vh) rotate(100deg); }
                        50%  { transform: translate(calc(-1 * var(--sway, 20px)), 50vh) rotate(200deg); }
                        75%  { transform: translate(var(--sway, 20px), 78vh) rotate(300deg); }
                        100% { transform: translate(0, 112vh) rotate(360deg); }
                    }
                    @keyframes flake-twinkle { from { opacity: 1; } to { opacity: 0.15; } }

                    /* ============ SPLASH SCREEN ============ */
                    #splash-screen {
                        position: fixed; top: 0; left: 0; width: 100%; height: 100%;
                        background: radial-gradient(circle at 50% 42%, rgba(35,26,48,.92), rgba(5,5,5,.96));
                        backdrop-filter: blur(15px);
                        z-index: 1000; display: flex; flex-direction: column;
                        align-items: center; justify-content: center;
                        overflow: hidden; transition: opacity 0.5s ease;
                        animation: splash-in 1.1s ease both;
                    }
                    @keyframes splash-in { from { opacity: 0; } to { opacity: 1; } }
                    #splash-screen.splash-out { animation: splash-exit 0.65s cubic-bezier(.55, 0, .55, .2) forwards; }
                    @keyframes splash-exit { to { opacity: 0; transform: scale(1.14); filter: blur(18px); } }

                    #splash-stars { position: absolute; inset: 0; overflow: hidden; pointer-events: none; }
                    .star {
                        position: absolute; border-radius: 50%; background: #fff;
                        box-shadow: 0 0 6px rgba(255,255,255,.8); opacity: 0;
                        animation: star-twinkle var(--td, 3s) ease-in-out var(--tl, 0s) infinite alternate;
                    }
                    @keyframes star-twinkle { from { opacity: 0.15; transform: scale(0.6); } to { opacity: 0.95; transform: scale(1.2); } }
                    .shooting-star {
                        position: absolute; width: 130px; height: 2px; border-radius: 2px;
                        background: linear-gradient(90deg, rgba(255,255,255,.95), transparent);
                        box-shadow: 0 0 8px rgba(255,255,255,.6);
                        opacity: 0; animation: shoot 8s linear infinite;
                    }
                    @keyframes shoot {
                        0%   { opacity: 0; transform: translate(0, 0) rotate(-32deg); }
                        2%   { opacity: 1; }
                        10%  { opacity: 0; transform: translate(46vw, 30vh) rotate(-32deg); }
                        100% { opacity: 0; transform: translate(46vw, 30vh) rotate(-32deg); }
                    }

                    .splash-title { font-size: 3.6rem; margin: 0 0 8px; }
                    .splash-sub {
                        margin: 0 0 55px; color: rgba(255,255,255,.85); font-size: 1rem;
                        text-transform: uppercase; opacity: 0; letter-spacing: 0.65em;
                        animation: track-in 1.5s cubic-bezier(.22, 1, .36, 1) 1s forwards;
                    }
                    @keyframes track-in {
                        from { opacity: 0; letter-spacing: 0.65em; transform: translateY(12px); }
                        to   { opacity: 0.85; letter-spacing: 0.22em; transform: translateY(0); }
                    }

                    .launch-wrap { position: relative; animation: rise-in 0.8s cubic-bezier(.22, 1, .36, 1) 1.1s both; }
                    .pulse-ring {
                        position: absolute; inset: -5px; border-radius: 60px;
                        border: 2px solid rgba(255,71,87,.55); pointer-events: none;
                        animation: ring-expand 2.4s cubic-bezier(.22, 1, .36, 1) infinite;
                    }
                    .pulse-ring.r2 { border-color: rgba(167,119,227,.45); animation-delay: 1.2s; }
                    @keyframes ring-expand { 0% { transform: scale(1); opacity: 0.8; } 100% { transform: scale(1.55); opacity: 0; } }

                    .launch-btn {
                        position: relative; padding: 25px 60px; font-size: 2rem; font-weight: 800;
                        color: white; background: linear-gradient(135deg, #ff4757, #ff6b81);
                        border: none; border-radius: 50px; cursor: pointer; overflow: hidden;
                        box-shadow: 0 0 20px rgba(255,71,87,.4), inset 0 0 10px rgba(255,255,255,.2);
                        transition: filter 0.3s ease;
                        animation: breathe 3.2s ease-in-out infinite alternate, btn-glow 2.6s ease-in-out infinite alternate;
                    }
                    @keyframes breathe { from { scale: 1; } to { scale: 1.045; } }
                    @keyframes btn-glow {
                        0%   { box-shadow: 0 0 18px rgba(255,71,87,.35), inset 0 0 10px rgba(255,255,255,.2); }
                        100% { box-shadow: 0 0 48px rgba(255,71,87,.75), 0 0 110px rgba(167,119,227,.35), inset 0 0 10px rgba(255,255,255,.2); }
                    }
                    .launch-btn:hover { transform: translateY(-4px) scale(1.06); filter: brightness(1.18) saturate(1.15); }
                    .launch-btn:active, .game-btn:active { transform: scale(0.97); }

                    .launch-btn::after, .game-btn::after {
                        content: ''; position: absolute; top: 0; left: -100%;
                        width: 50%; height: 100%;
                        background: linear-gradient(90deg, transparent, rgba(255,255,255,.4), transparent);
                        transform: skewX(-20deg); transition: left 0.5s;
                    }
                    .game-btn:hover::after { left: 150%; }
                    .launch-btn::after { animation: auto-shine 5s ease-in-out infinite; }
                    @keyframes auto-shine { 0% { left: -100%; } 55% { left: 150%; } 100% { left: 150%; } }

                    /* ============ MAIN UI ============ */
                    .ui-container {
                        position: relative; z-index: 10; display: flex; flex-direction: column;
                        align-items: center; justify-content: center; height: 100%;
                        background: rgba(0, 0, 0, 0.3); backdrop-filter: blur(8px);
                    }

                    h1.neon-title {
                        font-size: 4.5rem; margin-bottom: 14px; font-weight: 900; line-height: 1.2;
                        text-transform: uppercase; text-align: center; padding: 0 16px;
                        background: linear-gradient(to right, #ff4757, #ff6b81, #6e8efb, #a777e3, #ff4757);
                        background-size: 300% 100%;
                        -webkit-background-clip: text; background-clip: text; color: transparent;
                        animation: bg-shift 6s linear infinite, title-glow 3.5s ease-in-out infinite alternate;
                    }
                    @keyframes bg-shift { 0% { background-position: 0% 50%; } 100% { background-position: 150% 50%; } }
                    @keyframes title-glow {
                        0%   { filter: drop-shadow(0 0 6px rgba(255,71,87,.25)); }
                        100% { filter: drop-shadow(0 0 22px rgba(167,119,227,.55)); }
                    }

                    /* Letter-by-letter masked reveal */
                    .ltr-mask { display: inline-block; overflow: hidden; }
                    .ltr { display: inline-block; transform: translateY(125%); }
                    .splash-title .ltr { transform: none; animation: ltr-in 0.85s cubic-bezier(.22, 1, .36, 1) var(--d, 0ms) both; }
                    body.ui-in .main-title .ltr { transform: none; animation: ltr-in 0.85s cubic-bezier(.22, 1, .36, 1) var(--d, 0ms) both; }
                    @keyframes ltr-in { from { transform: translateY(125%) rotate(8deg); } to { transform: translateY(0) rotate(0deg); } }

                    /* Animated gradient line under the title */
                    .title-line {
                        width: min(520px, 78vw); height: 3px; border-radius: 3px; margin: 0 0 34px;
                        background: linear-gradient(90deg, transparent, #ff4757, #a777e3, #6e8efb, transparent);
                        background-size: 200% 100%;
                        transform: scaleX(0);
                    }
                    body.ui-in .title-line {
                        transform: scaleX(1);
                        animation: line-grow 0.9s cubic-bezier(.22, 1, .36, 1) 1.1s both,
                                   line-shimmer 3s linear 2s infinite alternate;
                    }
                    @keyframes line-grow { from { transform: scaleX(0); opacity: 0; } to { transform: scaleX(1); opacity: 1; } }
                    @keyframes line-shimmer { from { background-position: 0% 0; } to { background-position: 100% 0; } }

                    .search-bar {
                        padding: 18px 30px; width: 450px; max-width: 85vw; border-radius: 40px;
                        border: 2px solid rgba(255,255,255,0.1); outline: none; font-size: 1.2rem;
                        background: rgba(255,255,255,0.1); color: white; backdrop-filter: blur(10px);
                        margin-bottom: 45px; box-shadow: 0 8px 32px rgba(0,0,0,0.3); opacity: 0;
                        transition: transform 0.3s ease, background 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
                    }
                    body.ui-in .search-bar:not(.entered) { opacity: 1; animation: rise-in 0.8s cubic-bezier(.22, 1, .36, 1) 1s both; }
                    .search-bar.entered { opacity: 1; }
                    .search-bar:focus { background: rgba(255,255,255,0.2); border-color: #6e8efb; box-shadow: 0 0 20px rgba(110,142,251,0.4); transform: scale(1.02); }
                    .search-bar.entered:focus { animation: focus-glow 2.4s ease-in-out infinite; }
                    @keyframes focus-glow {
                        0%, 100% { box-shadow: 0 0 16px rgba(110,142,251,.35), 0 8px 32px rgba(0,0,0,.3); border-color: rgba(110,142,251,.6); }
                        50%      { box-shadow: 0 0 38px rgba(110,142,251,.7), 0 8px 32px rgba(0,0,0,.3); border-color: #6e8efb; }
                    }
                    @keyframes rise-in { from { opacity: 0; translate: 0 32px; } to { opacity: 1; translate: 0 0; } }

                    .buttons-grid { display: flex; gap: 20px; flex-wrap: wrap; justify-content: center; max-width: 900px; }

                    .game-btn {
                        position: relative; padding: 20px 40px; font-size: 1.2rem; font-weight: bold;
                        text-transform: uppercase; letter-spacing: 1px; color: white;
                        background:
                            radial-gradient(circle at var(--mx, 50%) var(--my, 50%), rgba(255,255,255,.28), transparent 55%),
                            linear-gradient(135deg, #6e8efb, #a777e3);
                        border: none; border-radius: 20px; cursor: pointer; overflow: hidden;
                        box-shadow: 0 10px 20px rgba(0,0,0,0.4);
                        transition: transform 0.18s ease-out, box-shadow 0.35s ease;
                        opacity: 0; will-change: transform;
                    }
                    body.ui-in .game-btn {
                        opacity: 1;
                        animation: btn-in 0.7s cubic-bezier(.22, 1, .36, 1) var(--d, 700ms) both,
                                   btn-float var(--fd, 5s) ease-in-out var(--fdelay, 1500ms) infinite alternate;
                    }
                    @keyframes btn-in {
                        from { opacity: 0; translate: 0 52px; scale: 0.85; filter: blur(14px); }
                        to   { opacity: 1; translate: 0 0;   scale: 1;    filter: blur(0px); }
                    }
                    @keyframes btn-float { from { translate: 0 0; } to { translate: 0 -8px; } }
                    .game-btn:hover { transform: translateY(-8px); box-shadow: 0 18px 40px rgba(110,142,251,.55), 0 0 30px rgba(167,119,227,.35); }

                    /* Rotating conic-gradient border (spins on hover) */
                    .game-btn::before {
                        content: ''; position: absolute; inset: 0; border-radius: inherit; padding: 2px;
                        background: conic-gradient(from var(--ang, 0deg), #6e8efb, #a777e3, #ff4757, #ff6b81, #6e8efb);
                        -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                        -webkit-mask-composite: xor;
                        mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
                        mask-composite: exclude;
                        opacity: 0.45; transition: opacity 0.35s ease;
                        animation: border-spin 3.2s linear infinite paused;
                        pointer-events: none;
                    }
                    .game-btn:hover::before { opacity: 1; animation-play-state: running; }
                    @keyframes border-spin { to { --ang: 360deg; } }

                    /* Click ripple */
                    .ripple {
                        position: absolute; border-radius: 50%; pointer-events: none;
                        background: radial-gradient(circle, rgba(255,255,255,.55) 0%, rgba(255,255,255,.15) 60%, transparent 70%);
                        transform: scale(0); opacity: 0.8;
                        animation: ripple-go 0.65s ease-out forwards;
                    }
                    @keyframes ripple-go { to { transform: scale(1); opacity: 0; } }

                    .settings-btn {
                        position: absolute; top: 30px; right: 30px; background: rgba(255,255,255,0.1);
                        border: 1px solid rgba(255,255,255,0.2); color: white; padding: 12px 25px;
                        border-radius: 30px; cursor: pointer; font-size: 1.1rem; backdrop-filter: blur(5px);
                        transition: all 0.3s ease; opacity: 0;
                    }
                    body.ui-in .settings-btn { opacity: 1; animation: rise-in 0.7s cubic-bezier(.22, 1, .36, 1) 0.8s both; }
                    .settings-btn:hover { background: rgba(255,255,255,0.2); transform: rotate(5deg) scale(1.05); }

                    /* ============ SETTINGS (springy pop-in) ============ */
                    #settings-backdrop {
                        position: fixed; inset: 0; z-index: 90; background: rgba(0,0,0,.55);
                        backdrop-filter: blur(5px); opacity: 0; pointer-events: none; transition: opacity 0.4s ease;
                    }
                    #settings-backdrop.open { opacity: 1; pointer-events: auto; }

                    #settings-menu {
                        position: absolute; top: 50%; left: 50%;
                        background: rgba(20, 20, 20, 0.95); padding: 40px; border-radius: 25px; z-index: 100;
                        width: 380px; max-width: 90vw;
                        box-shadow: 0 20px 50px rgba(0,0,0,.8), 0 0 40px rgba(110,142,251,.12);
                        border: 1px solid rgba(255,255,255,0.1);
                        opacity: 0; visibility: hidden; pointer-events: none;
                        transform: translate(-50%, -46%) scale(0.85);
                        transition: transform 0.5s cubic-bezier(.34, 1.56, .64, 1), opacity 0.35s ease, visibility 0s linear 0.35s;
                    }
                    #settings-menu.open {
                        opacity: 1; visibility: visible; pointer-events: auto;
                        transform: translate(-50%, -50%) scale(1);
                        transition: transform 0.5s cubic-bezier(.34, 1.56, .64, 1), opacity 0.35s ease;
                    }
                    .setting-group {
                        display: flex; flex-direction: column; margin-bottom: 25px;
                        opacity: 0; transform: translateY(16px);
                        transition: opacity 0.4s ease, transform 0.45s cubic-bezier(.22, 1, .36, 1);
                    }
                    #settings-menu.open .setting-group { opacity: 1; transform: translateY(0); }
                    #settings-menu.open .setting-group:nth-of-type(1) { transition-delay: 0.12s; }
                    #settings-menu.open .setting-group:nth-of-type(2) { transition-delay: 0.2s; }
                    #settings-menu.open .setting-group:nth-of-type(3) { transition-delay: 0.28s; }
                    .setting-group input { padding: 12px; margin-top: 8px; border-radius: 10px; border: none; background: #333; color: white; outline: none; transition: background 0.3s, box-shadow 0.3s; }
                    .setting-group input:focus { background: #444; box-shadow: 0 0 14px rgba(255, 71, 87, 0.35); }

                    .close-btn {
                        background: linear-gradient(135deg, #ff4757, #ff6b81); color: white; border: none;
                        padding: 15px; width: 100%; border-radius: 10px; cursor: pointer; font-weight: bold;
                        font-size: 1.1rem; transition: transform 0.2s;
                    }
                    .close-btn:hover { transform: scale(1.02); }

                    @media (max-width: 720px) {
                        h1.neon-title { font-size: 2.4rem; }
                        .splash-title { font-size: 2.6rem; }
                        .launch-btn { font-size: 1.4rem; padding: 20px 40px; }
                        .game-btn { padding: 16px 26px; font-size: 1rem; }
                    }
                    @media (prefers-reduced-motion: reduce) {
                        *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; }
                    }
                    /*]]>*/
                </style>
            </head>

            <body>
                <!-- Splash Screen -->
                <div id="splash-screen">
                    <div id="splash-stars"></div>
                    <h1 class="neon-title splash-title" id="splash-title">BE BETTER</h1>
                    <p class="splash-sub">Nigeria Reborn</p>
                    <div class="launch-wrap">
                        <span class="pulse-ring"></span>
                        <span class="pulse-ring r2"></span>
                        <button class="launch-btn" id="launch-app-btn">LAUNCH DASHBOARD</button>
                    </div>
                </div>

                <audio id="bg-audio" loop="loop"></audio>

                <video autoplay="autoplay" muted="muted" loop="loop" playsinline="playsinline" id="video-background">
                    <source src="https://motionbgs.com/media/758/mountain-landscape-in-autumn.960x540.mp4" type="video/mp4" />
                </video>

                <div id="orb-layer">
                    <div class="orb o1"></div>
                    <div class="orb o2"></div>
                    <div class="orb o3"></div>
                </div>

                <div id="snow-container"></div>

                <div id="fx-grid"></div>
                <div id="vignette"></div>
                <div id="mouse-glow"></div>

                <div class="ui-container" id="main-ui">
                    <button class="settings-btn" onclick="toggleSettings(true)">⚙️ Options</button>
                    <h1 class="neon-title main-title" id="main-title">BE BETTER Nigeria reborn</h1>
                    <div class="title-line"></div>
                    <input type="text" id="searchBar" class="search-bar" placeholder="Search quick access games..." onkeyup="filterGames()" />
                    <div class="buttons-grid" id="game-buttons">
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/GM.html">GN-math</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/EGP.html">EGP</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/MC1.8.html">Minecraft</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/RB.html">Retrobowl</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/RBC.html">Retrobowl College</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/TOTM.html">TOTM</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/soundboard.html">Soundboard</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/CC3D.html">crazy cattle 3d</button>
                        <button class="game-btn" data-url="https://cdn.jsdelivr.net/gh/carlosfnf/get-better@main/GDD.html">geomatrydash</button>
                    </div>
                </div>

                <div id="settings-backdrop" onclick="toggleSettings(false)"></div>

                <div id="settings-menu">
                    <h2 style="margin-top: 0; color: #ff4757;">Options</h2>
                    <div class="setting-group">
                        <label>Background URL (Image/MP4):</label>
                        <input type="text" id="bg-input" placeholder="Paste link here..." />
                    </div>
                    <div class="setting-group">
                        <label>Falling PNG URL (Snowflake):</label>
                        <input type="text" id="snow-input" placeholder="Paste PNG URL here..." />
                    </div>
                    <div class="setting-group">
                        <label>Local Song File (MP3/WAV):</label>
                        <input type="file" id="audio-file-input" accept="audio/*" />
                    </div>
                    <button class="close-btn" onclick="saveSettings()">Save &amp; Close</button>
                </div>

                <script>
                    //<![CDATA[
                    /* ================================================================
                       SEAMLESS LOADER (enhanced: shimmer text + progress bar)
                    ================================================================ */
                    const loaderOverlayDOM = `
                    <style>
                        #seamless-loader-overlay { position: fixed !important; top: 0 !important; left: 0 !important; width: 100vw !important; height: 100vh !important; background: radial-gradient(circle at 50% 45%, #16161f 0%, #050505 100%) !important; z-index: 2147483647 !important; display: flex !important; flex-direction: column !important; align-items: center !important; justify-content: center !important; transition: opacity 1.2s cubic-bezier(0.25, 0.8, 0.25, 1) !important; pointer-events: none !important; margin: 0 !important; padding: 0 !important; overflow: hidden !important; }
                        .cool-spinner { position: relative !important; width: 120px !important; height: 120px !important; margin-bottom: 40px !important; animation: loader-in .6s cubic-bezier(.22,1,.36,1) both !important; }
                        .cool-spinner div { position: absolute !important; box-sizing: border-box !important; border-radius: 50% !important; border: 4px solid transparent !important; animation: ring-spin 2s cubic-bezier(0.68, -0.55, 0.27, 1.55) infinite !important; }
                        .cool-spinner div:nth-child(1) { width: 100% !important; height: 100% !important; top: 0 !important; left: 0 !important; border-top-color: #ff4757 !important; border-bottom-color: #ff6b81 !important; }
                        .cool-spinner div:nth-child(2) { width: 80% !important; height: 80% !important; top: 10% !important; left: 10% !important; border-left-color: #6e8efb !important; border-right-color: #a777e3 !important; animation-direction: reverse !important; animation-duration: 1.5s !important; }
                        .cool-spinner div:nth-child(3) { width: 60% !important; height: 60% !important; top: 20% !important; left: 20% !important; border-top-color: #ffffff !important; animation-duration: 1s !important; }
                        @keyframes ring-spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
                        .loader-text { font-family: 'Segoe UI', Tahoma, sans-serif !important; font-size: 1.8rem !important; font-weight: 800 !important; letter-spacing: 6px !important; text-transform: uppercase !important; background: linear-gradient(90deg, #ff4757, #a777e3, #6e8efb, #ff4757) !important; background-size: 200% auto !important; -webkit-background-clip: text !important; background-clip: text !important; color: transparent !important; animation: pulse-glow 2s ease-in-out infinite alternate !important, text-shimmer 2.5s linear infinite !important, loader-in .6s .15s both !important; }
                        @keyframes pulse-glow { 0% { filter: drop-shadow(0 0 5px rgba(255,71,87,0.4)); } 100% { filter: drop-shadow(0 0 20px rgba(110,142,251,0.8)); } }
                        @keyframes text-shimmer { 0% { background-position: 0% 50%; } 100% { background-position: 200% 50%; } }
                        .loader-bar { width: 250px !important; max-width: 70vw !important; height: 4px !important; border-radius: 4px !important; background: rgba(255,255,255,0.1) !important; overflow: hidden !important; margin-top: 32px !important; position: relative !important; animation: loader-in .6s .3s both !important; }
                        .loader-bar::before { content: '' !important; position: absolute !important; top: 0 !important; left: -45% !important; width: 45% !important; height: 100% !important; border-radius: 4px !important; background: linear-gradient(90deg, #ff4757, #a777e3, #6e8efb) !important; box-shadow: 0 0 14px rgba(110,142,251,0.9) !important; animation: bar-slide 1.2s cubic-bezier(.45,0,.55,1) infinite !important; }
                        @keyframes bar-slide { 0% { left: -45%; } 100% { left: 100%; } }
                        @keyframes loader-in { from { opacity: 0; transform: translateY(24px); } to { opacity: 1; transform: translateY(0); } }
                    </style>
                    <div id="seamless-loader-overlay">
                        <div class="cool-spinner"><div></div><div></div><div></div></div>
                        <div class="loader-text" id="dynamic-loader-text">LOADING...</div>
                        <div class="loader-bar"></div>
                    </div>`;

                    const loaderScript = `
                    <script>
                        window.addEventListener('load', function() {
                            setTimeout(function() {
                                var overlay = document.getElementById('seamless-loader-overlay');
                                if(overlay) { overlay.style.opacity = '0'; setTimeout(function() { overlay.remove(); }, 1200); }
                            }, 600);
                        });
                        setTimeout(function() {
                            var overlay = document.getElementById('seamless-loader-overlay');
                            if(overlay && overlay.style.opacity !== '0') { overlay.style.opacity = '0'; setTimeout(function() { overlay.remove(); }, 1200); }
                        }, 4000);
                    <\/script>`;

                    function openSeamlessWindow(htmlContent, loadingMsg) {
                        var newWindow = window.open('about:blank', '_blank');
                        if(!newWindow) { alert("Popup blocked! Please allow popups for this site."); return; }

                        var customLoaderDOM = loaderOverlayDOM.replace('LOADING...', loadingMsg);
                        var initialHtml = '<!DOCTYPE html><html><head><title>Loading...</title></head><body style="margin:0; background:#050505;">' + customLoaderDOM + '</body></html>';
                        newWindow.document.write(initialHtml);

                        var finalHtml = htmlContent;
                        var injection = customLoaderDOM + loaderScript;

                        if (finalHtml.match(/<body[^>]*>/i)) {
                            finalHtml = finalHtml.replace(/(<body[^>]*>)/i, '$1' + injection);
                        } else if (finalHtml.includes('</head>')) {
                            finalHtml = finalHtml.replace('</head>', '</head>' + injection);
                        } else {
                            finalHtml = injection + finalHtml;
                        }

                        setTimeout(function() {
                            newWindow.document.open();
                            newWindow.document.write(finalHtml);
                            newWindow.document.close();

                            setTimeout(function() {
                                var clonedSplash = newWindow.document.getElementById('splash-screen');
                                if (clonedSplash) clonedSplash.remove();
                            }, 50);
                        }, 1200);
                    }

                    /* ================================================================
                       SILENT BACKGROUND PRE-CACHER (unchanged)
                    ================================================================ */
                    function preCacheGames() {
                        console.log("Starting silent background pre-cache...");
                        var buttons = document.querySelectorAll('.game-btn');
                        buttons.forEach(function(btn) {
                            var gameUrl = btn.getAttribute('data-url');
                            if (!localStorage.getItem('cache_' + gameUrl)) {
                                fetch(gameUrl)
                                    .then(r => r.text())
                                    .then(htmlCode => {
                                        try {
                                            localStorage.setItem('cache_' + gameUrl, htmlCode);
                                            console.log("Successfully cached for offline use: " + gameUrl);
                                        } catch (e) {
                                            console.warn("Storage limit reached for Chromebook. Could not cache: " + gameUrl);
                                        }
                                    })
                                    .catch(err => {
                                        console.log("Offline or network block. Could not pre-cache: " + gameUrl);
                                    });
                            }
                        });
                    }
                    window.addEventListener('load', preCacheGames);

                    /* ================================================================
                       ANIMATION HELPERS
                    ================================================================ */

                    // Split title text into individually-animated letters
                    function splitLetters(el) {
                        if (!el || el.dataset.split) return;
                        el.dataset.split = '1';
                        var text = el.textContent;
                        el.textContent = '';
                        var n = 0;
                        for (var i = 0; i < text.length; i++) {
                            var ch = text.charAt(i);
                            if (ch === ' ') { el.appendChild(document.createTextNode(' ')); continue; }
                            var mask = document.createElement('span');
                            mask.className = 'ltr-mask';
                            var ltr = document.createElement('span');
                            ltr.className = 'ltr';
                            ltr.textContent = ch;
                            ltr.style.setProperty('--d', (450 + n * 45) + 'ms');
                            mask.appendChild(ltr);
                            el.appendChild(mask);
                            n++;
                        }
                    }
                    splitLetters(document.getElementById('splash-title'));
                    splitLetters(document.getElementById('main-title'));

                    // Twinkling starfield + shooting stars on the splash screen
                    function initStars() {
                        var c = document.getElementById('splash-stars');
                        if (!c || c.dataset.init) return;
                        c.dataset.init = '1';
                        for (var i = 0; i < 70; i++) {
                            var star = document.createElement('span');
                            star.className = 'star';
                            var sz = (Math.random() * 2.2 + 1).toFixed(1);
                            star.style.width = sz + 'px';
                            star.style.height = sz + 'px';
                            star.style.left = (Math.random() * 100).toFixed(2) + '%';
                            star.style.top = (Math.random() * 100).toFixed(2) + '%';
                            star.style.setProperty('--td', (1.6 + Math.random() * 3).toFixed(2) + 's');
                            star.style.setProperty('--tl', (Math.random() * 4).toFixed(2) + 's');
                            c.appendChild(star);
                        }
                        for (var j = 0; j < 2; j++) {
                            var sh = document.createElement('span');
                            sh.className = 'shooting-star';
                            sh.style.top = (Math.random() * 35 + 5).toFixed(1) + '%';
                            sh.style.left = (Math.random() * 35 + 5).toFixed(1) + '%';
                            sh.style.animationDelay = (1.5 + j * 3.8) + 's';
                            c.appendChild(sh);
                        }
                    }
                    initStars();

                    /* ================================================================
                       GAME BUTTONS: 3D tilt + cursor spotlight + ripple + float vars
                    ================================================================ */
                    var gameButtons = document.querySelectorAll('.game-btn');
                    gameButtons.forEach(function (btn, i) {
                        var delay = 700 + i * 85;
                        btn.style.setProperty('--d', delay + 'ms');
                        btn.style.setProperty('--fdelay', (delay + 800) + 'ms');
                        btn.style.setProperty('--fd', (4 + Math.random() * 3).toFixed(2) + 's');

                        btn.addEventListener('mousemove', function (e) {
                            var r = this.getBoundingClientRect();
                            var x = (e.clientX - r.left) / r.width;
                            var y = (e.clientY - r.top) / r.height;
                            this.style.setProperty('--mx', (x * 100).toFixed(1) + '%');
                            this.style.setProperty('--my', (y * 100).toFixed(1) + '%');
                            this.style.transform = 'perspective(700px) rotateX(' + ((y - 0.5) * -10).toFixed(2) + 'deg) rotateY(' + ((x - 0.5) * 12).toFixed(2) + 'deg) translateY(-6px) scale(1.03)';
                        });
                        btn.addEventListener('mouseleave', function () { this.style.transform = ''; });

                        btn.addEventListener('click', function (e) {
                            this.style.transform = '';
                            var rect = this.getBoundingClientRect();
                            var ripple = document.createElement('span');
                            ripple.className = 'ripple';
                            var size = Math.max(rect.width, rect.height) * 2.2;
                            ripple.style.width = ripple.style.height = size + 'px';
                            ripple.style.left = (e.clientX - rect.left - size / 2) + 'px';
                            ripple.style.top = (e.clientY - rect.top - size / 2) + 'px';
                            this.appendChild(ripple);
                            setTimeout(function () { ripple.remove(); }, 700);
                        });
                    });

                    /* ================================================================
                       LAUNCH TRIGGERS
                    ================================================================ */
                    document.getElementById('launch-app-btn').addEventListener('click', function() {
                        // Reveal the UI (triggers the staggered entrance cascade)
                        document.body.classList.add('ui-in');
                        // Serialize BEFORE marking the splash as exiting, so the cloned
                        // dashboard re-plays its entrance animations in the new window
                        var fullDashboardHTML = '<!DOCTYPE html>\n' + document.querySelector('html').outerHTML;

                        var splash = document.getElementById('splash-screen');
                        splash.classList.add('splash-out');
                        setTimeout(function() { splash.style.display = 'none'; }, 700);

                        openSeamlessWindow(fullDashboardHTML, 'PLEASE WAIT');
                    });

                    gameButtons.forEach(function(btn) {
                        btn.addEventListener('click', function() {
                            btn.style.transform = '';
                            var gameUrl = btn.getAttribute('data-url');
                            var cachedHtml = localStorage.getItem('cache_' + gameUrl);
                            var baseUrlPath = gameUrl.substring(0, gameUrl.lastIndexOf('/') + 1);
                            var baseTag = `<base href="${baseUrlPath}">`;

                            if (cachedHtml) {
                                openSeamlessWindow(baseTag + cachedHtml, 'LAUNCHING GAME...');
                            } else {
                                var newWindow = window.open('about:blank', '_blank');
                                if(!newWindow) return alert("Popup blocked! Allow popups.");

                                var customLoaderDOM = loaderOverlayDOM.replace('LOADING...', 'FETCHING DATA...');
                                newWindow.document.write('<!DOCTYPE html><html><head><title>Loading...</title></head><body style="margin:0; background:#050505;">' + customLoaderDOM + '</body></html>');

                                fetch(gameUrl)
                                    .then(r => r.text())
                                    .then(htmlCode => {
                                        try { localStorage.setItem('cache_' + gameUrl, htmlCode); } catch(e){}
                                        var finalHtml = htmlCode;
                                        var injection = customLoaderDOM.replace('FETCHING DATA...', 'LAUNCHING GAME...') + loaderScript;

                                        if (finalHtml.includes('<head>')) {
                                            finalHtml = finalHtml.replace('<head>', '<head>' + baseTag);
                                        }

                                        if (finalHtml.match(/<body[^>]*>/i)) {
                                            finalHtml = finalHtml.replace(/(<body[^>]*>)/i, '$1' + injection);
                                        } else {
                                            finalHtml = injection + finalHtml;
                                        }

                                        setTimeout(() => {
                                            newWindow.document.open();
                                            newWindow.document.write(finalHtml);
                                            newWindow.document.close();
                                        }, 800);
                                    })
                                    .catch(err => {
                                        newWindow.document.open();
                                        newWindow.document.write('<body style="background:black;color:red;display:flex;justify-content:center;align-items:center;height:100vh;font-family:sans-serif;"><h1>FAILED TO LOAD GAME. CHECK CONNECTION.</h1></body>');
                                        newWindow.document.close();
                                    });
                            }
                        });
                    });

                    /* ================================================================
                       UI, SETTINGS & EFFECTS
                    ================================================================ */
                    var bgAudio = document.getElementById('bg-audio');
                    document.body.addEventListener('click', function() {
                        if (bgAudio.src && bgAudio.paused) bgAudio.play().catch(e => {});
                    }, { once: true });

                    function filterGames() {
                        var query = document.getElementById('searchBar').value.toLowerCase();
                        gameButtons.forEach(function(btn) {
                            btn.style.display = btn.innerText.toLowerCase().includes(query) ? "block" : "none";
                        });
                    }

                    // Search bar: hand off from entrance animation to focus-glow animation
                    document.getElementById('searchBar').addEventListener('animationend', function (e) {
                        if (e.animationName === 'rise-in') this.classList.add('entered');
                    });

                    // Animated "typing" placeholder
                    (function typePlaceholder() {
                        var el = document.getElementById('searchBar');
                        if (!el) return;
                        var text = 'Search quick access games...';
                        var i = 0;
                        function tick() {
                            el.setAttribute('placeholder', text.slice(0, i) + (i < text.length ? '_' : ''));
                            if (i <= text.length) { i++; setTimeout(tick, 55); }
                            else { setTimeout(function () { i = 0; tick(); }, 4200); }
                        }
                        tick();
                    })();

                    var settingsMenu = document.getElementById('settings-menu');
                    var settingsBackdrop = document.getElementById('settings-backdrop');
                    function toggleSettings(show) {
                        settingsMenu.classList.toggle('open', show);
                        settingsBackdrop.classList.toggle('open', show);
                    }
                    document.addEventListener('keydown', function (e) {
                        if (e.key === 'Escape') toggleSettings(false);
                    });

                    var currentSnowPng = "data:image/svg+xml;utf8,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' stroke='rgba(255,255,255,0.9)' stroke-width='2' stroke-linecap='round'%3E%3Cpath d='M12 2v20M2 12h20M5 5l14 14M5 19L19 5'/%3E%3C/svg%3E";
                    var videoBackground = document.getElementById('video-background');

                    function saveSettings() {
                        var bgUrl = document.getElementById('bg-input').value;
                        var snowUrl = document.getElementById('snow-input').value;
                        var audioFileInput = document.getElementById('audio-file-input');

                        if (bgUrl.trim() !== "") {
                            if (bgUrl.toLowerCase().endsWith('.mp4') || bgUrl.toLowerCase().endsWith('.webm')) {
                                videoBackground.src = bgUrl; videoBackground.style.display = 'block'; document.body.style.backgroundImage = 'none';
                            } else {
                                document.body.style.backgroundImage = 'url("' + bgUrl + '")';
                                document.body.style.backgroundSize = 'cover'; document.body.style.backgroundPosition = 'center';
                                videoBackground.style.display = 'none';
                            }
                        }
                        if (snowUrl.trim() !== "") currentSnowPng = snowUrl;

                        if (audioFileInput.files && audioFileInput.files[0]) {
                            bgAudio.src = URL.createObjectURL(audioFileInput.files[0]);
                            bgAudio.play().catch(e => {});
                        }
                        toggleSettings(false);
                    }

                    /* ================================================================
                       SNOW: depth-based size / speed / blur, organic sway, twinkle
                    ================================================================ */
                    function spawnSnowflake() {
                        var container = document.getElementById('snow-container');
                        if (!container) return;
                        var flake = document.createElement('img');
                        flake.src = currentSnowPng;
                        flake.classList.add('snowflake');

                        var depth = Math.random();          // 0 = far away, 1 = close to camera
                        var size = 7 + depth * 22;
                        var dur = 12.5 - depth * 6;         // near flakes fall faster
                        flake.style.width = size.toFixed(1) + 'px';
                        flake.style.height = size.toFixed(1) + 'px';
                        flake.style.left = (Math.random() * 100).toFixed(2) + 'vw';
                        flake.style.setProperty('--sway', ((Math.random() * 36 + 12) * (Math.random() < 0.5 ? -1 : 1)).toFixed(0) + 'px');
                        flake.style.setProperty('--o', (0.25 + depth * 0.6).toFixed(2));
                        flake.style.setProperty('--fdur', dur.toFixed(2) + 's');
                        flake.style.filter = 'blur(' + ((1 - depth) * 1.4).toFixed(2) + 'px) drop-shadow(0 0 5px rgba(255,255,255,.7))';
                        if (Math.random() < 0.35) {
                            flake.classList.add('twinkle');
                            flake.style.setProperty('--tdur', (1.2 + Math.random() * 1.4).toFixed(2) + 's');
                        }
                        container.appendChild(flake);
                        setTimeout(function() { flake.remove(); }, dur * 1000 + 400);
                    }

                    if (!window.snowIntervalStarted) {
                        for (var s = 0; s < 8; s++) setTimeout(spawnSnowflake, s * 120); // opening flurry
                        setInterval(spawnSnowflake, 350);
                        window.snowIntervalStarted = true;
                    }

                    /* ================================================================
                       MOUSE GLOW + SUBTLE UI PARALLAX (lerped, buttery smooth)
                    ================================================================ */
                    (function () {
                        var glow = document.getElementById('mouse-glow');
                        var ui = document.querySelector('.ui-container');
                        var tx = window.innerWidth / 2, ty = window.innerHeight / 2, gx = tx, gy = ty;
                        var tax = 0, tay = 0, pax = 0, pay = 0;
                        window.addEventListener('mousemove', function (e) {
                            tx = e.clientX; ty = e.clientY;
                            tax = e.clientX / window.innerWidth - 0.5;
                            tay = e.clientY / window.innerHeight - 0.5;
                        });
                        (function loop() {
                            gx += (tx - gx) * 0.09;
                            gy += (ty - gy) * 0.09;
                            pax += (tax - pax) * 0.05;
                            pay += (tay - pay) * 0.05;
                            if (glow) glow.style.transform = 'translate(' + (gx - 200).toFixed(1) + 'px,' + (gy - 200).toFixed(1) + 'px)';
                            if (ui) ui.style.transform = 'translate(' + (pax * 18).toFixed(2) + 'px,' + (pay * 12).toFixed(2) + 'px)';
                            requestAnimationFrame(loop);
                        })();
                    })();
                    //]]>
                </script>
            </body>
        </html>
    </foreignObject>
</svg>
